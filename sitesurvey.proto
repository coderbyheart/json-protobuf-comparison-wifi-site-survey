package asset_tracker_v2;
syntax = "proto3";

message WiFiSiteSurvey {
    uint32 ts = 1;
    repeated AP v = 2; 
}

message AP {
    int64 mac = 1;
    string ssid = 2;
    int32 rssi = 3;
    int32 chan = 4;
}